import React from 'react';
import Navbar from './Navbar/Navbar';

const Header = () => {
    return (
        <section id='header'>
            <Navbar />
        </section>
    );
};

export default Header;